{
  "name": "@family-ledger/cloud-ledger",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean",
    "test": "vitest run --passWithNoTests",
    "bundle": "node scripts/bundle.mjs",
    "schema:plan": "npm run build && node scripts/schema-plan.mjs",
    "deployment:check": "node scripts/deployment-contract.mjs",
    "r12d:smoke": "npm run build && node scripts/cloudbase-smoke.mjs"
  },
  "dependencies": {
    "@family-ledger/calc": "0.1.0",
    "@family-ledger/shared": "0.1.0",
    "@cloudbase/node-sdk": "^3.9.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "esbuild": "^0.21.5"
  }
}

# Copy to .env and fill in. Never commit real values.

# Tencent CloudBase environment id (from CloudBase console)
CLOUDBASE_ENV_ID=your-env-id

# Canonical timezone for interest "natural day" boundaries. Do not change
# without regenerating golden test vectors — see packages/calc.
LEDGER_TIMEZONE=Asia/Shanghai
